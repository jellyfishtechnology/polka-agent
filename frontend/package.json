{
  "name": "polka-agent-ui",
  "version": "1.0.0",
  "description": "PolkaAgent Marketplace - AI Agents on Polkadot",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "@polkadot/api": "^10.11.1",
    "@polkadot/api-contract": "^10.11.1",
    "@polkadot/extension-dapp": "^0.47.1",
    "@polkadot/util": "^12.6.1",
    "@polkadot/util-crypto": "^12.6.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
```

4. **Commit message:** `Add frontend package.json`
5. Click **"Commit new file"**

---

### ✅ DOPO IL PRIMO FILE

Dopo aver creato `frontend/package.json`, **torna alla root del repo** e verifica che vedi:
```
polka-agent/
├── agent_registry/
├── ai_agents/
├── frontend/           ✅ NUOVA!
│   └── package.json    ✅
├── .gitignore
└── README.md
